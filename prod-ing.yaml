apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-ing
  namespace: prod
spec:
  rules:
  - host: prod.local
    http:
      paths:
      - path: /
        pathType: Prefix   
        backend:
          service:
            name: prod
            port:
              number: 80

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  name: prod-ing
  namespace: prod
spec:
  rules:
  - host: prod
    http:
      paths:
      - path: /
        pathType: Prefix   
        backend:
          service:
            name: prod-blue-svc
            port:
              number: 80
        path: /
        pathType: Exact
status:
  loadBalancer: {}
